<nav id="mainNav" class="navbar navbar-expand-md navbar-dark container mx-auto">
  <a class="navbar-brand" href="/urls">TinyApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
    aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" href="/urls">My URLs</a>
      <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
    </div>

    

    <% if (user) { %>
      <div>
        <p>
          Welcome <span><%= user.username %>: </span> <%= user.email %>
        </p>
      </div>
      <div class="navTop ml-auto d-flex">
        <div>
          <form class="ml-3" method="POST" action="/user/logout">
            <button type="submit" class="btn btn-danger">Logout</button>
          </form>
        </div>
      </div>
      
      <% } else { %>
      <div class="ml-auto navTop">
        <button 
            class="btn btn-primary" 
            type="button"
            data-toggle="modal"
            data-target="#signInModal"
        >
          Sign In
        </button>
        <button>
          <a href="/user/signup">
            Sign Up
          </a>
        </button>
      </div>
    <% } %>
    
  </div>
</nav>


<%- include('sign_in_modal') -%>